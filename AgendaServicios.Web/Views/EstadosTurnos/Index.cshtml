@model IEnumerable<AgendaServicios.Web.Models.EstadosTurno>

@{
    ViewData["Title"] = "Index";
}

<h1>Estados Turnos</h1>

<p>
    <a asp-action="Create">Crear Nuevo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                    <a asp-action="Edit" class="btn btn-primary" asp-route-id="@item.EstadoTurnoId">Editar</a> |
                    <a asp-action="Details" class="btn btn-warning" asp-route-id="@item.EstadoTurnoId">Detalles</a> |
                <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.EstadoTurnoId">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
